# 開發注意事項

1. 每個獨立功能完成後，若有檔案變更則執行 `git commit`，並使用清楚描述該功能的提交訊息；若沒有檔案變更則不需提交。
2. 程式行為改變時，新增或更新必要的相關測試，確保新行為及受影響的既有功能有適當覆蓋。
3. 交付前執行與本次改動相關的測試；涉及共用核心、跨模組影響或發布時，才執行完整測試。所有執行的測試與驗證皆須通過；若有失敗，不得宣告完成。
4. 僅在使用方式、介面、設定或架構改變時，更新相關 Markdown（`.md`）文件。
5. 工作完成後，在 `開發進度.md` 以本地時間戳（含時區偏移）記錄工作摘要、變更檔案、驗證結果與 commit；避免重複記載完整操作過程。
