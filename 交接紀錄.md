# 交接紀錄（2026-09-12，第二輪）

給下一個接手的 agent：這份取代前一份交接紀錄（多 PDF 支援那輪），記錄這次對話做了什麼、目前狀態、以及還沒做完或需要留意的事。前一輪的多 PDF 架構改動都已經是目前的基礎，不再重複說明。

## 這次做了什麼

### 1. 使用者提出的 7 項 UI 需求（依原始編號）
1. **移除 Groq 文字**（連線設定頁）：改成「預設使用 OpenAI」，因為免費 Groq 流量不夠用。
2. **上一頁/下一頁 → 上一份文件/下一份文件**（PDF 與參數頁）：拿掉逐頁瀏覽，改成在已加入的多份 PDF 之間切換，一次顯示該文件全部 chunk。新函式：`switch_document` / `previous_document` / `next_document`，取代舊的 `preview_page` / `previous_page` / `next_page`。
3. **移除「解析起始頁/結束頁」設定**：每份 PDF 一律解析全文，`add_document_for_ui` 不再接受 start/end page 參數。
4. **移除「最大實體/關係類型數」「最大輸出 tokens」設定**（建圖頁）：一開始三個都拿掉、改用固定常數（`DEFAULT_MAX_OUTPUT_TOKENS=4096`、`DEFAULT_MAX_ENTITY_TYPES=15`、`DEFAULT_MAX_RELATIONSHIP_TYPES=20`）。**後來「最大實體/關係類型數」兩個又復原成可調整的 UI 欄位**（見下方「事後修正」），只有「最大輸出 tokens」維持拿掉。
5. 多文件 Schema 整合策略：使用者要求跳過，沒做。
6. **評測題目標註來源文件**（自動問答測試頁）：`generate_evaluation_questions` 現在要求模型回傳 `source_chunk_numbers`，程式再反查 chunk 對應的文件名稱寫入 `document` 欄位；題目表格新增「來源文件」欄。
7. **修好「無法測試題目」**：`run_evaluation_for_ui` 呼叫 `answer_question_for_ui` 時漏傳 `embedding_api_base`/`embedding_api_key`，導致參數全部錯位、一鍵測試必定 `TypeError`。已修正並補上防回歸測試（驗證轉發的參數完全正確）。

### 2. 多檔上傳狀態訊息改條列式
一次上傳多份 PDF 時，`add_document_for_ui` 的狀態文字從「全部擠在一行用空白分隔」改成每份文件一個 `- ✅…` 條列項目（單一檔案上傳維持原本單行格式不變）。

### 3. 常駐「目前專案」橫幅
標題下方新增 `current_project_banner`（`gr.Markdown`），不管切到哪個頁籤都會顯示「📁 目前專案：XXX」，透過 `project_state.change()` 自動更新（建立/載入專案時更新、刪除專案後變回「尚未選擇」）。

### 4. 建圖流程的可靠性修正（`graph_service.py`）
使用者拿 55 份 PDF、9584～9919 個 chunk 實測時連續踩到幾個問題，依序修好：

- **HTTP 429（rate limit）沒有重試**：`_post_json` 原本一撞到限流就直接整批失敗。現在會依 OpenAI 回應的 `Retry-After`（或自算的指數退避，上限 30 秒）自動重試，最多 `RATE_LIMIT_MAX_RETRIES=6` 次才真正報錯。
- **429 重試時的連線洩漏**：第一版重試邏輯忘記在重試前 `close()` 掉失敗的 `HTTPError` 回應物件，長時間跑下來會累積一堆 `CLOSE-WAIT` 的殘留連線，這很可能是使用者反應「跑很久、沒進度、也不報錯」的真正原因。已修正（重試前呼叫 `exc.close()`）。
- **重試時完全沒有可見狀態**：加了 `on_retry` callback，一路從 `_post_json` → `_chat_json` → `plan_batch`/`merge_group`/`extract_batch`，讓重試會即時把「第 X/Y 批遇到速率限制，Z 秒後重試（第 N/6 次）」推進 Gradio 的進度條文字，不會再看起來像卡住。
- **實體/關係類型上限復原成可調整**：55 份文件太雜，固定寫死的「最大關係類型數 20」在整合階段常常擠不進去導致失敗。使用者選擇「恢復可調整的 UI 設定」，已復原（見上方第 4 項）。

### 5. 新增「暫停 / 停止」控制
建圖頁的「分析文件並規劃 Schema」按鈕下方新增「⏸ 暫停」「⏹ 停止」，Schema 規劃跟抽取共用同一組控制：

- 新增 `RunControl`（`threading.Event` 包裝，含 `check()` / `wait_if_paused()` / `sleep()` 等方法）與 `RunCancelled` exception，定義在 `graph_service.py`。
- 一路傳進 `plan_graph_schema` / `extract_graph` / `_chat_json` / `_post_json`，在每個 batch/group worker 一開始、以及限流重試等待時檢查。
- **重要限制**：無法強制中斷一個「已經發出去、還在等回應」的 HTTP 請求；暫停/停止只保證「不會再送出新的批次」。停止時 `ThreadPoolExecutor` 的 `__exit__` 預設會等所有已排入/進行中的 worker 完成才真正返回（因為 worker 一開始就先檢查 `control.check()`，沒開始跑的會立刻丟出 `RunCancelled`，所以等待時間有界，通常是「目前這一輪已發出的請求跑完」的時間，不是全部批次的時間）。
- `gr.State(RunControl())` 的初始值需要能被 `deepcopy`（Gradio 每個 session 都會複製一份），但 `threading.Event` 不能 pickle/deepcopy，所以 `RunControl` 加了 `__deepcopy__` 回傳全新的 `RunControl()`。
- 暫停/停止按鈕用 `queue=False` 綁定，繞過 `app.py` 裡 `.queue(default_concurrency_limit=1)` 的限制，確保 Schema 規劃還在跑的時候按下去能立刻生效，不用排隊等。

## 目前狀態
- 分支 `0912_fixed`，本機 HEAD 在 `c12c5cd`（比 `origin/0912_fixed` 領先 1 個 commit，`main` 落後 `0912_fixed` 1 個 commit）。**這個環境沒有 `gh` CLI、也沒有設定 push 憑證**（`~/.git-credentials` 不存在），所以這次沒有實際開 PR，只有把內容準備好。
- 這輪的變更同樣被自動用通用訊息 `"files update"` commit 掉了（commit `c12c5cd`），不是我主動 commit 的，跟前一輪一樣的模式。**下一個 agent 開始前務必先 `git status` / `git log` 確認狀態。**
- `pytest`：128 個測試全過，無已知失敗（前一輪遺留的 `test_build_app_has_automatic_evaluation_page` 測試 bug——targets 用 list 比對 tuple 導致恆假——這次也順手修掉了）。
- 本機 Gradio 服務跑在 `http://127.0.0.1:7860`（PID 11634），是套用最新程式碼的版本。**這次過程中因為要套用程式碼改動重啟了服務非常多次**，每次都會清空所有瀏覽器分頁的 Gradio session 狀態（`chunk_state`/`documents_state`/`project_state` 全部歸零，即使底層 `data/projects/<id>/project.json` 的資料完全沒事）。如果下一個 agent 又改了 `ui.py`/`graph_service.py`，記得提醒使用者改動後要回「0. 專案設定」重新點「載入專案」才能恢復畫面上的狀態。
- 使用者實測用的專案：`data/projects/SM3E-1L33U3`（55 份文件、9919 chunk）、`data/projects/SM11X00-1K12E0E`（41 份文件、9584 chunk）。兩個專案本身健康（檔案都在、chunk 數量對得起來），只是規劃/抽取因為量體大、原本又沒有重試機制才一直失敗，現在應該已經可以順利跑完（使用者尚未回報最終是否成功跑完整個 55 份文件的建圖，下一個 agent 如果使用者回報還有問題，先確認是不是新的 rate limit 額度問題還是別的）。

## 已知限制 / 沒做的事
1. **多文件 Schema 整合策略**（原始需求第 5 項）：使用者要求先跳過，沒有處理「一次幫多個文件建圖要不要分別建 schema 再整合」的問題。
2. **跨文件同名實體合併**：前一輪就有的已知限制，這次沒有進一步處理。
3. **`py-spy` 這類 process 內部除錯工具在這個沙盒環境用不了**（`ptrace` 權限被擋，`sudo` 也一樣），之後如果又遇到「看起來卡住但不知道卡在哪」的狀況，只能靠 `ss -tnp`（連線狀態）、`ps`（CPU 時間）這類外部觀察去推斷，沒辦法直接拿到 Python thread 的 stack trace。
4. **暫停/停止是「盡快」而不是「立刻」**：使用者要注意這個語意，已經在 UI 文字裡說明過，但如果之後有人抱怨「按了停止還是等了一下子」，這是設計上的必然限制，不是 bug。
5. **正式 PR 沒有開**：只準備了 PR 標題/內文文字，因為環境沒有 `gh` CLI 也沒有 push 憑證。如果使用者要真的開 PR，需要使用者自己在有憑證的環境執行 `git push`，或是提供憑證讓下一個 agent 執行。
6. **這個執行環境沒有 GitHub push 憑證**：跟前一輪一樣，任何需要 push 到遠端的操作都要請使用者在別的地方執行。

## 建議下一步（如果使用者沒有新指示）
- 先確認上面「目前狀態」還準不準（git log、測試結果、7860 服務是否還活著、有沒有被使用者自己動過）。
- 如果使用者要接著跑大量 PDF 建圖，可以留意這次新加的暫停/停止/重試機制是否真的解決問題，必要時再依實際回報調整 `RATE_LIMIT_MAX_RETRIES`／退避時間，或建議使用者調整 OpenAI 帳號的 rate limit tier。
