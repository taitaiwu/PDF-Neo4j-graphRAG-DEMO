# 交接紀錄

## 專案現況

- Gradio 單機 PDF GraphRAG 工具，程式位於 `src/manual_graphrag/`。
- 支援多 PDF、Schema 規劃、圖譜抽取、Neo4j 混合檢索、問答及自動評測。

## 頁面流程

- 0 專案設定、1 連線設定、2 PDF 與參數、3 PDF 摘要。
- 4 建圖、5 自動問答測試、6 問答測試、7 歷史紀錄。
- 第 3 頁獨立建立各 PDF 摘要，保存於 `evaluation.document_summaries`。
- 自動測試由 LLM 依問題及摘要選 PDF，再限制 Neo4j 檢索、圖譜擴展及回答只能使用所選文件。

## 文件識別資訊

- 摘要包含 `summary`、`primary_identifier`、`identifiers`、`topics`、`keywords`。
- `primary_identifier` 優先使用主要對象的共同或系列名稱；`identifiers` 最多 5 項。
- 提示排除出版商、作者、文件編號、平台、一般欄位及附帶名稱。
- 舊 `product_names` 仍可讀取；舊摘要需手動重新建立才會套用新規則。

## 生題與評測

- 每份 PDF 各生 N 題；「允許並行」勾選後不同 PDF 各自並行，同一 PDF 仍逐題依序請求；未勾選時全部 PDF 依序生題。
- 題目必須包含主要識別名稱；沒有時使用第一項舊識別資訊或 PDF 名稱。
- 生題目前每題一次模型請求，長文件較慢；後續可改成整批生題、只補缺額。
- 測試可選擇是否使用本機 Reranker；結果統計路由正確、回答正確、兩者皆正確、Recall@5 與 MRR。
- 每題的 retrieval rank、Recall@5 命中狀態及 reciprocal rank 會保存於專案；重新載入時由保存結果重算並顯示完整摘要。

## 設定

- API endpoint、API key 與 Neo4j 憑證放在 `.env`，不寫入 `project.json`。
- `config/model_settings.yaml` 保存模型清單；LLM 有 6 個用途，舊 5 欄設定會自動補摘要欄位。
- Chat completions 不設定 `max_tokens`，實際限制由模型服務決定。
- `MODEL_OLLAMA_API_BASE` 的 `chat/completions` 一律使用 SSE 內部串流收集；完成後重組為一般回應，未完成的串流會整次重試。OpenAI Chat Completions 與所有 Embedding 請求不受影響。

## 開發與驗證

- 遵守 `AGENTS.md`：行為變更補測試、測試通過後提交，commit 使用類型前綴。
- 相關測試：`tests/test_evaluation_service.py`、`tests/test_ui.py`、`tests/test_service_settings.py`。
