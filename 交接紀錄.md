# 交接紀錄（2026-09-12）

給下一個接手的 agent：這份紀錄整理這次對話做了什麼、目前狀態、以及還沒做完或需要留意的事。

## 這次做了什麼

### 1. 合併兩個資料夾
把 `/home/taitaiwu/wakaba_PDF/PDF-Neo4j-graphRAG-DEMO`（remote: `wakaba0972/PDF-Neo4j-graphRAG-DEMO`）合併進本專案
`/home/taitaiwu/PDF-Neo4j-graphRAG-DEMO`（remote: `taitaiwu/PDF-Neo4j-graphRAG-DEMO`，是 wakaba0972 的 fork）。

- 用暫時 remote + `git merge` 完成，解決了 2 處衝突（`ui.py` import、`tests/test_ui.py` 測試呼叫參數）。
- 合併時發現並修正一個**既有 bug**（`preferences._display_retrieval_mode(get(...))` 應為
  `_display_retrieval_mode(preferences.get(...))`），這個 bug 在 wakaba_PDF 原始版本裡本來就存在，不是合併造成的。
- 把 wakaba_PDF 裡 gitignore 的本機測試資料（`data/projects/test1/`、5 個 `data/qa/*.json`）複製進來，沒有覆蓋既有檔案。
- **`/home/taitaiwu/wakaba_PDF/PDF-Neo4j-graphRAG-DEMO` 資料夾使用者選擇先保留，之後手動刪**——不要主動刪除它。
- `.env` 內容使用者自己手動調整過（Groq → OpenAI 設定），目前是一致的（`EMBEDDING_API_BASE`/`EMBEDDING_API_KEY` 都指向 OpenAI，跟 `EMBEDDING_MODEL="text-embedding-3-small"` 搭配正確）。

### 2. 多 PDF 支援（一個專案可讀取多份 PDF）
橫跨以下檔案，把「一個專案 = 一份 PDF」的架構改成「一個專案 = 多份 PDF 合併成一張圖」：

- `chunking.py`：`TextChunk` 新增 `document` 欄位；`chunk_pages()` 支援 `document=`、`start_number=`，讓多份 PDF 的 chunk 全域連續編號、不衝突。
- `graph_service.py`：`_merge_sources()` 讓實體/關係抽取結果累積 `source_documents`（同名實體跨文件合併時會記錄來源）。
- `neo4j_service.py`：`source_documents` 貫穿 `_write_graph()` 的 entity/relation/evidence 寫入，以及 `load_latest_graph()`、`search_graph_evidence()` 的所有 Cypher 投影（4 處）。
- `project_store.py`：`document`（單一 dict）→ `documents`（list），`save_project()` 改接受 `document_paths: list[str]` 並累加而非覆蓋；`load_project()` 對舊格式專案自動遷移（不會壞掉既有資料）；新增 `remove_document(project_id, file_name)`。
- `ui.py`：大量重構，重點狀態：
  - `documents_state`（list，累積所有已加入 PDF 的 metadata）
  - `active_preview_state` / `active_chunks_state`（只給「頁碼瀏覽」用，永遠對應最後加入的那份文件）
  - `chunk_state`（list，跨所有文件累積的 TextChunk）
  - 新函式：`add_document_for_ui`（取代舊的 `preview_pdf`）、`_add_single_document`（單檔處理邏輯）、`remove_document_for_ui`、`_document_rows`、`_document_choices`
  - `save_project_for_ui` / `load_project_for_ui` 的參數與回傳 tuple 都改了（**順序很重要**，跟 `project_setting_inputs` / `project_load_outputs` 兩個 list 要完全對應，改動時要小心數對）

### 3. 一次多選上傳
`pdf_file` 元件改成 `file_count="multiple"`，`add_document_for_ui` 改成迴圈處理多個路徑，單一檔案失敗（例如同名重複）不會讓整批失敗，只會在狀態訊息裡標出。

### 4. 移除已加入的 PDF（可多選）
新增「選擇要移除的 PDF」下拉選單（`multiselect=True`）+「移除選定的 PDF」按鈕：
- 呼叫 `project_store.remove_document()` 刪除硬碟上複製的檔案並更新專案登記
- 同步修剪記憶體中的 `documents_state` / `chunk_state`
- 移除後自動改顯示剩餘文件中最後加入的那份作為 active document

### 5. Git / GitHub 狀況
- 本次改動被**使用者自己（或其他 session/工具）**用通用訊息 `"files update"` 自動 commit 掉了（commit `60f6fe2` = 多 PDF 支援、`f0c05d2` = 移除功能），不是我主動 commit 的。**下一個 agent 開始前務必先 `git status` / `git log` 確認目前狀態，不要假設有未 commit 的東西。**
- 幫使用者寫過兩次 PR 內容（純文字 markdown，貼在對話裡），沒有實際用 `gh` 開 PR（這個環境沒有 `gh` CLI，也沒有 GitHub push 憑證）。
- **GitHub「Sync fork」事件**：使用者不小心點了 fork 的 Sync fork，GitHub 幫他把上游 `wakaba0972/main` 的 2 個 commit merge 進 `taitaiwu/main`（merge commit `782b618`，內容是 `6161376 fix: load evaluation retrieval preference` 和 `15c9618 fix: keep PDF page range valid before upload`）。這是安全的 merge（原本的 commit 都還在，是父節點），不是破壞性覆蓋。使用者要求復原。我因為這個環境沒有 push 憑證，只能把指令給使用者自己執行：
  ```
  cd /home/taitaiwu/PDF-Neo4j-graphRAG-DEMO
  git push origin main --force-with-lease
  ```
  **✅ 已確認完成**：截至這份紀錄產生時（2026-09-12），重新 `git fetch` 確認 `origin/main` 已經回到 `f0c05d2`，`782b618` 已經不在任何分支上了，代表使用者已經自己執行過這個指令、GitHub 上的 fork 已經復原乾淨。**這件事不需要下一個 agent 再處理。**

## 目前狀態

- 本機分支 `main`，HEAD 在 `f0c05d2`，`origin/main` 一致，working tree 乾淨。
- 測試：`pytest` 一致是 110 passed / 1 failed。那個失敗是 `tests/test_ui.py::test_build_app_has_automatic_evaluation_page`，**這是合併過來的既有 bug，不是這次改動造成的**（在 wakaba_PDF 原始版本裡本來就會失敗）。沒有修，需要深入研究 Gradio 的 `save_evaluation_questions_for_ui` 事件綁定（綁定到 `question_table` 的 `"input"` 事件目前沒接上）才能修。
- 本機有一個 Gradio 服務跑在 `http://127.0.0.1:7860`（PID 5787，用 `.venv/bin/python src/app.py` 啟動），目前是套用最新程式碼的版本。如果下一個 agent 又改了 `ui.py`／相關檔案，記得要重啟這個 process 才會生效（`kill <pid>` 再重新 `nohup .venv/bin/python src/app.py &`）。

## 已知限制 / 沒做的事

1. **跨文件同名實體合併**：目前用「型別+名稱（不分大小寫）」判斷是否為同一實體，跨 PDF 合併時可能誤判成同一個東西（例如兩份手冊剛好都有叫「電源」的元件，但其實是不同裝置）。已經口頭跟使用者說過這個風險，沒有進一步處理，如果之後要做更嚴謹的實體消歧，這是要留意的地方。
2. **`test_build_app_has_automatic_evaluation_page` 失敗**：既有 bug，未修。
3. **`wakaba_PDF` 資料夾**：使用者說要自己手動刪，不要主動處理。
4. **正式 PR 沒有開**：只有草稿文字，沒有建分支、沒有 push、沒有用 `gh pr create`。如果使用者要求開 PR，需要先確認要開去哪個 base（`taitaiwu/main` 還是 `wakaba0972/main`）。
5. **這個執行環境沒有 GitHub push 憑證**（無 `gh` CLI、無 credential helper、無 `~/.git-credentials`）。任何需要 push 到遠端的操作都要請使用者在別的地方執行，或請使用者先設定好憑證。

## 建議下一步（如果使用者沒有新指示）
- 如果使用者繼續談這個專案，先確認上面「目前狀態」還準不準（尤其是 git log、測試結果、7860 服務是否還活著），因為時間可能已經過去一段時間，狀態可能被使用者自己動過。
