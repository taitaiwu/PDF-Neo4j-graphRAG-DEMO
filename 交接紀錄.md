# 交接紀錄（2026-09-13，第三次更新）

本節記錄目前程式行為與驗證結果；下方 2026-09-13（第二次、第一次）與 2026-09-12 的內容僅保留作為歷史背景，其中分支、HEAD、測試數量與部分行為描述均已過時。

## 本輪（第三次更新）完成項目

這輪是接著第二次更新，實際啟動 App 並在瀏覽器/本機 Ollama 上跑過一輪真實測試後，修掉測試中發現的問題：

1. **修正問答紀錄寫入專案歷史的欄位錯位 bug（正確性問題，非本輪引入，這次才發現並修好）**：`answer_question_for_project_ui`（`src/manual_graphrag/ui.py`）用位置索引 `args[N]` 組出要存進專案 `questions` 的 record，但索引跟 `answer_question_for_ui` 目前的參數順序（含 `embedding_api_base`／`embedding_api_key`）對不上，整整差了 2 格。結果是：`question` 欄位被誤填成 Neo4j 密碼、`answer_model` 被誤填成 Neo4j 使用者名稱、`retrieval_mode` 被誤填成回答模型名稱，而 `top_k` 欄位是 `int(問題文字)`——只要問題不是純數字字串就會丟 `ValueError`，導致存檔動作每次都失敗（使用者會看到「✅ 回答成功」但「⚠️ 回答成功，但專案紀錄保存失敗」，因為顯示位置不顯眼很容易被忽略，實際現象是「6. 歷史紀錄」永遠不會多東西）。已修正索引對齊到 `answer_model=args[8]`、`question=args[9]`、`retrieval_mode=args[10]`、`top_k=args[11]`；`tests/test_ui.py::test_project_answer_appends_history` 原本用少 2 個參數的呼叫方式意外「巧合通過」蓋掉了這個 bug，這次已改成傳完整 12 個參數並新增對 `question`／`answer_model`／`retrieval_mode`／`top_k` 四個欄位的斷言，之後再錯位會直接測試失敗。
2. **聊天型請求逾時從 120 秒調高到 600 秒**：拿掉 `max_tokens`／`max_output_tokens` 上限後，本機／區網的小模型（例如透過遠端 Ollama 呼叫 `llama3.2:1b`、`gemma4:e4b`）生成沒有長度上限的回應時，很容易超過原本寫死在 `_post_json`（`src/manual_graphrag/graph_service.py`）的 120 秒逾時，導致 Schema 規劃／抽取／自動問答生題／問答回覆都可能報 `無法連線模型 API：timed out`，即使模型本身沒有卡住、只是還在生成。新增常數 `CHAT_COMPLETION_TIMEOUT_SECONDS = 600`，`_post_json` 的預設 `timeout` 改用這個常數；`qa_service.py` 的 embedding／回答呼叫沒有另外指定 timeout，一樣吃到新的預設值。這是本次拿掉 token 上限後**確認會發生的真實副作用**，不是假設。
3. **模型下拉選單在沒有可用模型時顯示提示**：「Schema 規劃 LLM」「知識圖譜抽取 LLM」「Embedding 模型」「生題模型」「回答與評判模型」「問答 LLM」六個下拉選單（`allow_custom_value=False`，只能從清單選），原本在沒有任何服務測試成功時就是單純空選單、沒有任何提示，使用者容易搞不清楚為什麼選不到東西。新增 `NO_MODEL_CHOICES_HINT` 常數與 `_model_choice_info()` helper，套用在這六個 Dropdown 的 `info=`，並接到 `render_service_for_ui`（連線測試／切換 Provider／取得模型清單後即時更新）與 `load_evaluation_with_services_for_ui`（載入專案時）；沒有可用模型時顯示「⚠️ 尚無可用模型，請先到「1. 連線設定」測試連線或取得模型清單。」，一旦有可用模型會自動清掉。新增測試 `test_render_service_hints_when_no_models_available`。
4. **釐清 `.env` 「編輯後又被還原」的成因（非 bug，但容易誤會，值得記錄）**：`build_app()`（`src/manual_graphrag/ui.py`）只在 `src/app.py` 啟動當下呼叫一次 `load_env()`，把當時 `.env` 的內容烤進頁面元件初始值；這個 Blocks 物件之後重複服務所有瀏覽器分頁，**不會**因為之後又編輯了 `.env` 檔案就重新讀取。連線設定頁幾乎每個欄位都掛了 `component.change(persist_env_settings, ...)`，只要有任何觸發 `.change` 的動作，就會把「瀏覽器記憶體裡的那份（可能是伺服器啟動當下的舊）值」整包寫回 `.env`，蓋掉使用者直接編輯檔案打進去的新值。**這不是這次改動造成的，是這個 App 一直以來的既有設計**；日後如果要手動編輯 `.env`，必須重啟 App 行程（或至少在瀏覽器按「重新讀取 .env」）才能讓新值生效並避免被蓋回去。

### 本輪順便做的環境操作（供下一位接手者知悉）

- 本機一開始有一個舊版程式碼的 Gradio 行程在跑（PID 11634），這次為了實測新程式碼把它 `kill` 掉並重新啟動；之後因為程式改動（逾時調整、下拉選單提示、歷史紀錄 bug 修正）又重啟了幾次。**目前跑的行程是 PID `18487`**，用 `nohup .venv/bin/python src/app.py` 背景啟動，log 導到 `/tmp/claude-197609/-home-taitaiwu/490d89d2-1f57-41ad-846c-c486838bd726/scratchpad/gradio_app.log`（這個路徑是 agent 的 scratchpad，不是專案的一部分，重開機或換一個 agent session 後不保證還在）。下一位接手者如果要繼續開發並重啟，記得先確認使用者手上是否有進行中的操作（可用 `ss -tnp | grep -E "26.8.126.215|neo4j.io|:7687"` 看有沒有連到 Ollama／Neo4j 的活躍連線）。
- 為了能在無 GUI 環境用瀏覽器實際點過一輪 UI（而不是只跑 mocked 單元測試），這次額外把 `playwright` 裝進 `.venv`、並執行 `playwright install --with-deps chromium`（含瀏覽器執行檔與系統套件，約 300MB+）。這兩者都**不在** `requirements.txt` 裡，純粹是這次驗證用的本機環境變更，沒有寫進任何專案設定檔。
- 測試過程中建立的 `data/projects/smoke-test-*` 測試專案已經清乾淨；`config/model_settings.yaml` 因為透過瀏覽器實際操作（切換 Provider、取得模型清單）會被 App 自動存檔，目前工作目錄的版本已經反映使用者自己後續的真實操作結果——**跟先前交接紀錄一樣，這個檔案的未提交變更不要跟著這次的程式碼一起 commit**，除非使用者另有指示。

## 上一輪（第二次更新）完成項目

1. **自動問答測試：跨 PDF 重複題目的最後把關**：原本並行生題時，每份 PDF 只會排除「自己」已生成的題目，不同 PDF 之間沒有互相比對，理論上可能生出重複或高度相似的題目。這次在 `generate_evaluation_for_ui`（`src/manual_graphrag/ui.py`）收集完所有 PDF 的題目後，新增一次全體兩兩比對（沿用既有的 `questions_are_similar`），若仍偵測到跨 PDF 重複會回傳明確錯誤訊息，不會靜默放行。新增測試 `test_generate_evaluation_detects_cross_document_duplicates`（`tests/test_ui.py`）。
2. **連線設定頁的頁面解鎖條件放寬**：`workflow_tabs_for_ui`（`src/manual_graphrag/ui.py`）原本要求 Neo4j、模型服務、Embedding 服務三者都連線成功才會解鎖第 2–6 頁；改為 Neo4j 仍是必要條件，但模型服務與 Embedding 服務只要任一個連線成功即可解鎖（實際建圖或問答時，若對應功能需要的服務未連線，仍會在該步驟報錯，不影響資料正確性）。對應測試更名為 `test_workflow_gate_requires_project_neo4j_and_any_service` 並更新斷言。
3. **README.md 補充說明**：更新「專案介紹」「基本使用流程」段落，補上目前「每份 PDF 生 N 題」的並行生題行為、跨 PDF 去重、每份 PDF 答對題數摘要，以及上述連線設定解鎖條件放寬的說明。Dockerfile 本身未變動——內容（安裝依賴、複製 `src/`／`config/`、健康檢查）與現況一致，不需要跟著這次的行為調整更新。
4. **移除輸出 token 上限後的驗證**：確認相關 mocked 測試（`tests/test_graph_service.py`、`tests/test_qa_service.py`、`tests/test_evaluation_service.py`、`tests/test_config.py`）全數通過，沒有因為拿掉 `max_tokens`／`max_output_tokens` 而出現迴歸。**沒有**對使用者的真實 OpenAI／Ollama 端點發送請求做端對端驗證，避免消耗使用者的 API 額度；如果需要真正跑一次 Schema 規劃／抽取確認大文件不再因輸出長度被截斷，需要使用者自己觸發或明確同意由 agent 代為測試。

## 既有失敗（第二次更新時發現，第三次更新後仍未處理）

- `pytest` 全量執行結果（第三次更新後）：`170 passed, 2 failed`（第三次更新新增 `test_render_service_hints_when_no_models_available`，並補強 `test_project_answer_appends_history` 的斷言，故較第二次更新時的 169 passed 多 1）。
- `tests/test_ui.py::test_pdf_page_range_has_valid_values_before_upload`：與先前交接紀錄記載的一樣，測試仍尋找已移除的「解析起始頁」元件，非本輪造成。
- `tests/test_service_settings.py::test_gradio_events_switch_connect_filter_and_restore`：第二次更新時才發現、先前交接紀錄未記載的既有失敗，第三次更新沒有處理。錯誤為 `StopIteration`，測試在呼叫 `event(component("現有專案")["id"], "focus")` 時找不到綁定在「現有專案」下拉選單上的 `focus` 事件；目前 `src/manual_graphrag/ui.py` 中「現有專案」（第 1509 行附近）沒有註冊任何 `.focus(...)` 事件處理。下一位接手者需要判斷：是該補上遺漏的 focus 事件（例如重新整理專案清單），還是測試本身已經過時要更新／移除。

## 最新完成項目

1. **設定與敏感資料分工**
   - `.env` 保存各服務的 API Base URL、API key 與 Neo4j 密碼等敏感資料。
   - `config/model_settings.yaml` 保存可選 provider／model 與作用中模型選擇。
   - `project.json` 只保存專案工作狀態；載入舊專案時會忽略、儲存時會移除 `model_endpoint` 與 `api_key`。
2. **模型輸出與知識圖譜限制**
   - Chat Completions 請求已完全移除 `max_tokens`；`max_output_tokens` 也已從函式介面、設定與 UI 狀態移除。
   - Schema 類型數量與抽取後的實體／關係數量沒有硬性上限，但仍以字元數分批送入模型，避免單次輸入超過 context window。
3. **專案選單初始化**
   - 未載入專案進入第一頁時，會依實際專案狀態刷新選單並清除不存在的舊值，不再出現 `Value ... is not in the list of choices`。
4. **自動問答評估**
   - 題數改為「每份 PDF 生 N 題」；選取多份文件時，總題數為文件數乘以 N。
   - 「生題最大並行請求數」控制可同時處理的 PDF 數；同一份 PDF 內逐題串行，前一題完成並加入排除清單後才送下一題。測試另有獨立並行數，兩者預設皆為 3。
   - 題目保留來源 PDF、來源頁碼與 chunk；測試結果顯示每份 PDF 的答對題數／N，以及總答對題數／總題數。

## 驗證與目前狀態

- 分支：`main`；上述功能最新提交：`bb70362 feat: generate questions per PDF and summarize results`。
- 最近一次相關評估測試：`19 passed, 47 deselected`。
- 移除輸出 token 設定後的相關回歸測試：`100 passed, 1 deselected`。
- 完整 UI 測試仍有一個既有過時案例 `test_pdf_page_range_has_valid_values_before_upload`：測試仍尋找已移除的「解析起始頁」元件，需要另行更新，不能把目前狀態描述成完整測試全數通過。
- 工作目錄中的 `config/model_settings.yaml` 是使用者既有變更，後續提交不得一併納入，除非使用者另有指示。

## Ollama 實測資訊

- 使用者目前端點：`http://26.8.126.215:11434`，模型：`gemma4:e4b`；OpenAI-compatible API Base URL 應使用尾端帶 `/v1` 的位址。
- 查詢模型資訊時，模型宣告的最大 context length 為 131072 tokens，但當時執行中的 Ollama context 為 4096；實際可用上限以 Ollama 啟動／模型載入設定為準。
- 移除 `max_tokens` 只代表客戶端不限制輸出，並不會移除 Ollama 的 context window、記憶體或模型自身限制。

## 接手注意事項

- 若要調高實際 context，應修改 Ollama 的 `OLLAMA_CONTEXT_LENGTH` 或載入參數並重新載入模型，再以 `/api/ps` 確認實際值。
- 每份 PDF 生 N 題會使模型請求量隨文件數線性增加；大量文件時應同時評估 N、並行數、模型吞吐量與記憶體。
- `改善計畫.md` 的 GraphRAG 檢索改善仍是獨立待辦，本輪未處理。

---

# 歷史交接紀錄（2026-09-12，第三輪）

給下一個接手的 agent：這份取代前一份交接紀錄（暫停/停止與重試機制那輪），記錄這次對話做了什麼、目前狀態、以及還沒做完或需要留意的事。前兩輪的架構改動都已經是目前的基礎，不再重複說明。

## 這次做了什麼

使用者要求：加入連線 Ollama 的功能，因為用 OpenAI 建圖太耗 token。

先確認了現況：`graph_service.py`／`qa_service.py` 對模型與 Embedding 服務本來就是走**通用 OpenAI 相容 REST API**（`{base}/chat/completions`、`{base}/embeddings`、`{base}/models`，Bearer token 可選），不是寫死呼叫 OpenAI SDK。也就是說架構上早就能指向 Ollama 的 OpenAI 相容端點（預設 `http://localhost:11434/v1`），**只是連線設定頁完全沒有提示或捷徑**，使用者得自己猜 endpoint 網址跟本機模型的確切 tag 名稱。這次的改動聚焦在補上這個 UX 缺口，沒有改動底層 HTTP 呼叫邏輯：

1. **`src/manual_graphrag/config.py`**：新增常數 `OLLAMA_DEFAULT_BASE_URL = "http://localhost:11434/v1"`。
2. **`src/manual_graphrag/graph_service.py`**：
   - 把 `check_model_connection` 內建的 GET 請求邏輯抽成共用 `_get_json(url, api_key, timeout=30)`。
   - 新增 `list_models(base_url, api_key) -> list[str]`：呼叫 `{base}/models`，解析回應的 `data[].id`，回傳排序去重後的模型 id 清單；沒有 `data` 陣列或清單為空都會丟 `ValueError`。這個函式同時對 OpenAI 與 Ollama 都適用（Ollama 的 OpenAI 相容層會把本機已 `ollama pull` 的模型列在這裡）。
3. **`src/manual_graphrag/ui.py`**（「1. 連線設定」頁）：
   - 新增「⚡ 套用 Ollama 本機預設（省 token）」按鈕：把「模型服務」與「Embedding 服務」的 API Base URL 都填成 `http://localhost:11434/v1`、API Key 清空，並直接呼叫 `persist_env_settings` 寫入 `.env`（對應函式 `apply_ollama_preset_for_ui`）。
   - 新增「重新整理模型清單」按鈕（模型服務區塊）：呼叫 `list_models`，把結果灌進「3. 建圖」「4. 自動問答測試」「5. 問答測試」頁面所有 LLM 選擇下拉選單的 `choices`（`graph_llm_model`／`extraction_llm_model`／`evaluation_generation_model`／`evaluation_test_model`／`answer_model`），並保留使用者原本選的值（對應函式 `refresh_llm_models_for_ui`）。
   - 新增「重新整理 Embedding 模型清單」按鈕（Embedding 服務區塊）：同樣邏輯，只更新 `graph_embedding_model` 下拉選單（對應函式 `refresh_embedding_models_for_ui`）。
   - 相關 Markdown 說明文字順便提到可接 Ollama 等 OpenAI 相容 API。
4. **測試**：`tests/test_graph_service.py` 新增 `list_models` 的成功/失敗案例與 `check_model_connection` 的錯誤案例；`tests/test_ui.py` 新增三個新函式的單元測試，以及「連線設定頁確實有這三顆新按鈕」的 `build_app` 結構測試。

## 目前狀態

- 分支 `0912_fixed`，本機 HEAD 在 commit `bcad488`（提交訊息：`feat: 支援連線本機 Ollama 以節省建圖 token 成本`）。這次是我自己主動執行的 `git commit`（依 `AGENTS.md` 的規則：獨立功能完成後要 commit），不是被自動流程用通用訊息代為 commit。
- `pytest`：**137 個測試全過**（原本 128 個 + 這次新增 9 個），無已知失敗。
- **這個沙盒環境沒有安裝 `ollama`**，也沒有本機 Ollama 服務可用，所以這次**沒有辦法對真正的 Ollama 伺服器做端對端連線測試**；驗證僅止於：
  - `list_models` / `check_model_connection` 針對假造的 HTTP 回應（`monkeypatch` 掉 `urllib.request.urlopen`）的單元測試全過。
  - `build_app()` 能正常建構，沒有拋例外，且連線設定頁確實含有三顆新按鈕與正確的 wiring。
  - **沒有**在真正的瀏覽器裡點過這幾顆新按鈕，也沒有用真的 Ollama 服務驗證「填入本機端點 → 建圖真的能跑起來」這條路徑。如果使用者本機有裝 Ollama，建議下一步就是請使用者實測：啟動 Ollama、`ollama pull` 一個聊天模型與一個 embedding 模型（例如 `llama3.1` 與 `nomic-embed-text`）、在「1. 連線設定」按「套用 Ollama 本機預設」再按「重新整理模型清單」，確認清單裡真的能選到剛 pull 的模型，並實際跑一次「規劃 Schema」確認能吃到 Ollama 回應。
- 本機仍有一個舊的 Gradio 服務跑在 `http://127.0.0.1:7860`（PID 11634，是**這次改動之前**的程式碼版本）。**這次沒有重啟它**，因為重啟會清空所有瀏覽器分頁的 Gradio session 狀態（同前兩輪紀錄提到的限制），而且使用者可能還在用它。如果使用者接下來想要在瀏覽器裡實際測試 Ollama 功能，需要重啟這個服務（`kill 11634` 後重新 `./start.sh` 或直接跑 `src/app.py`），下一個 agent 執行前務必先確認使用者知情、且已經把重要的專案狀態存檔（實際資料在 `data/projects/<id>/project.json`，重啟不會遺失，只是畫面要重新載入專案）。

## 已知限制 / 沒做的事

1. **沒有針對 Ollama 做任何 prompt 或參數層級的相容性調整**：例如沒有加上 `response_format: {"type": "json_object"}`，也沒有針對本機小模型調整 `max_output_tokens` 預設值或重試策略。現有的 JSON 解析與「二次要求修正」重試機制（`_chat_json` 裡的邏輯）理論上兩邊都通用，但本機模型（尤其小尺寸模型）遵循「只輸出 JSON」指令的穩定度可能不如 GPT 系列，如果使用者反應 Ollama 建圖常常要重試或抽取品質差，可以考慮在偵測到 Ollama 端點時自動加 `response_format` 或提高預設 `max_output_tokens`。
2. **模型清單抓取假設 Ollama 有實作 `GET /v1/models`**：這是 Ollama OpenAI 相容層的標準端點，新版都有支援；如果使用者的 Ollama 版本太舊沒有這個端點，「重新整理模型清單」會顯示連線失敗，但「套用 Ollama 本機預設」＋手動在下拉選單打模型名稱（`allow_custom_value=True`，本來就支援）仍然可用。
3. **Embedding 相容性未驗證**：Ollama 需要另外 `ollama pull` 一個支援 embedding 的模型（例如 `nomic-embed-text`、`mxbai-embed-large`）才能用 `/v1/embeddings`；一般聊天模型不一定支援 embedding 端點。這點沒有在 UI 上特別提示，只在 Markdown 裡簡單帶過，如果使用者卡在這裡可以加強提示文字。
4. **`改善計畫.md` 裡的 GraphRAG 檢索改善（P0～P7）跟這次改動無關**，沒有觸碰，仍是待辦。
5. **這個執行環境沒有 GitHub push 憑證**：跟前兩輪一樣，任何需要 push 到遠端的操作都要請使用者在別的地方執行。

## 建議下一步（如果使用者沒有新指示）

- 請使用者實際用本機 Ollama 測試一次「套用 Ollama 本機預設」→「重新整理模型清單」→ 選模型 →「分析文件並規劃 Schema」的完整流程，確認真的能跑、而且比 OpenAI 省 token／省錢。
- 依實測回報，評估是否要處理上面「已知限制」第 1、3 點（JSON 相容性、Embedding 模型提示）。
- 如果使用者要接著跑大量 PDF 建圖，本機模型的推論速度通常遠低於 OpenAI API，注意 `RATE_LIMIT_MAX_RETRIES`／timeout 等參數是否還適用（Ollama 不會回 429，但本機大 context 推論可能非常慢，這次沒有針對這點調整任何 timeout 設定）。
