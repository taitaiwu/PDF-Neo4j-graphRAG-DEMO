# 開發進度

本文件用於依時間順序記錄每次工作的目標、執行內容、驗證結果與完成狀態。時間戳統一採用本地時間與 ISO 8601 格式，並包含時區偏移；目前使用台北時間（UTC+08:00）。

## 2026-09-06T22:26:17+08:00

### 工作目標

- 建立集中記錄每次工作進度的 `開發進度.md`。
- 在 `AGENTS.md` 新增規範，要求每次工作完成後留下帶有時間戳的詳細進度紀錄。

### 完成內容

- 新增 `開發進度.md`，定義紀錄用途與時間戳格式。
- 在 `AGENTS.md` 的「開發注意事項」新增第 5 項規範。
- 建立本次工作的首筆紀錄，包含工作目標、完成內容、驗證方式及結果。

### 驗證

- 確認 `AGENTS.md` 的既有規範保持不變，且新增規範編號連續、內容清楚。
- 確認 `開發進度.md` 包含有效的本地 ISO 8601 時間戳、時區偏移與完整工作資訊。
- 本次僅修改 Markdown 文件，未變更程式碼，因此沒有需要新增或執行的程式測試。

### 結果

- 文件建立與規範更新已完成，後續工作須遵循新增的進度紀錄要求。

## 2026-09-06T22:29:14+08:00

### 工作目標

- 將開發進度的時間戳由 UTC 改為本地時間。

### 完成內容

- 更新 `AGENTS.md` 第 5 項規範，明確要求使用含時區偏移的本地時間戳。
- 將 `開發進度.md` 的時間戳格式說明改為本地時間，並指定目前採用台北時間（UTC+08:00）。
- 將前次紀錄的 UTC 時間戳換算為對應的台北本地時間。

### 驗證

- 確認所有進度標題均符合含時區偏移的 ISO 8601 格式。
- 確認原時間 `2026-09-06T14:26:17Z` 正確換算為 `2026-09-06T22:26:17+08:00`。
- 本次僅修改 Markdown 文件，未變更程式碼，因此沒有需要新增或執行的程式測試。

### 結果

- 開發進度現已使用台北本地時間，且時間戳會清楚標示 UTC+08:00 時區偏移。

## 2026-09-06T22:38:23+08:00

### 工作目標

- 查核 PDF 頁面解析功能是否已實作，並確認目前可用範圍與測試狀態。

### 查核內容

- 檢視 `src/manual_graphrag/pdf_service.py` 的逐頁文字擷取實作。
- 檢視 `src/manual_graphrag/ui.py` 的 PDF 預覽與 chunk 串接流程。
- 搜尋現有測試、需求文件、目錄說明及相關 Git 提交紀錄。
- 使用專案虛擬環境執行完整 pytest 測試套件。

### 驗證

- `.venv/bin/pytest -q`：18 項測試全部通過，另有 1 項 Gradio 棄用警告。
- 確認功能可驗證副檔名、拒絕損毀或加密 PDF、逐頁擷取文字、保留頁碼並列出無文字頁面。
- 確認目前沒有針對 `pdf_service.py` 的直接單元測試，也不支援掃描型 PDF 的 OCR。

### 結果

- PDF 頁面解析功能已完成初版並接入操作介面，但解析服務的直接測試覆蓋與 OCR 仍待補強。

## 2026-09-06T22:45:11+08:00

### 工作目標

- 評估將 PDF 讀取套件由 `pypdf` 改為 PyMuPDF4LLM 的可行性。

### 評估內容

- 對照現有 `extract_pdf()`、`PageText`、空白頁回報及 chunk 流程的介面需求。
- 查核 PyMuPDF4LLM 官方 API 的逐頁 chunk、Markdown、OCR 與頁面選取能力。
- 確認 Python 版本需求及安裝時帶入的 PyMuPDF、PyMuPDF Layout 相依套件。
- 檢視官方授權條款，辨識 AGPL v3 與商業授權對專案發布方式的影響。

### 驗證

- 官方 `to_markdown(..., page_chunks=True)` 可回傳各頁文字及頁面 metadata，能映射到現有資料結構。
- 本次僅進行可行性評估，未修改程式碼或相依套件，因此未執行程式測試。

### 結果

- 技術上可以替換，並可改善版面、表格及掃描頁處理；實作前需先確認授權策略與 OCR 部署需求。

## 2026-09-06T22:49:28+08:00

### 工作目標

- 查核在新分支測試 PyMuPDF4LLM 後，被 Git 忽略的環境與檔案是否會影響原分支。

### 查核內容

- 檢視 `.gitignore`、`requirements.txt`、目前分支及所有被忽略的工作區項目。
- 區分 Git 追蹤檔案與分支間共用的未追蹤／忽略檔案行為。
- 評估 PyMuPDF4LLM 安裝或升級相依套件對既有 `.venv` 的影響。

### 驗證

- 確認 `.venv/`、`.env`、`data/`、Python 快取及 pytest 快取均被 Git 忽略。
- 確認目前位於 `main`，且 `requirements.txt` 仍使用 `pypdf>=5.0,<6.0`。
- 本次僅進行 Git 與環境行為查核，未修改程式碼，因此未執行程式測試。

### 結果

- 切換分支不會還原共用工作目錄中的忽略檔案；測試 PyMuPDF4LLM 應使用獨立虛擬環境，避免污染原分支環境。

## 2026-09-06T23:10:15+08:00

### 工作目標

- 將 PDF 讀取與解析套件由 `pypdf` 正式改為 PyMuPDF4LLM。

### 完成內容

- 將 `requirements.txt` 的 PDF 相依套件改為 `pymupdf4llm>=1.28,<2.0`。
- 重構 `extract_pdf()`，使用 PyMuPDF4LLM 逐頁輸出 Markdown 並保留來源頁碼。
- 保留副檔名、加密 PDF、空白頁及異常解析結果的既有防護。
- 統一將文件開啟與 Markdown 轉換失敗回報為清楚的中文錯誤。
- 新增 PDF 解析服務單元測試與真實兩頁 PDF 整合測試。
- 同步更新 `README.md`、`目錄說明.md` 與 `開發計畫.md` 的技術、測試、OCR 及授權說明。

### 驗證

- `.venv/bin/pytest -q tests/test_pdf_service.py`：6 項初始解析服務測試全部通過。
- `.venv/bin/pytest -q`：26 項完整測試全部通過，另有 1 項既有 Gradio 棄用警告。
- `.venv/bin/python -m compileall -q src tests`：通過。
- `.venv/bin/pip check`：通過，未發現相依衝突。
- `git diff --check`：通過。

### 結果

- PDF 解析器已改用 PyMuPDF4LLM 1.28 系列並輸出逐頁 Markdown；OCR 暫時停用，現有文字型 PDF 流程與錯誤提示維持可用。

## 2026-09-06T23:17:37+08:00

### 工作目標

- 診斷 300 多頁 PDF 在網頁 chunk 表格中只顯示第 1–4 頁是否正常。

### 查核內容

- 檢視 PDF 解析、chunk 產生、預覽資料轉換與 Gradio 表格串接流程。
- 確認 `preview_rows()` 預設僅回傳前 10 個 chunk。
- 確認 UI 狀態文字會另外顯示完整解析頁數與完整 chunk 數量。

### 驗證

- 使用 20 個模擬 chunk 驗證預覽固定回傳前 10 筆，結果通過。
- `.venv/bin/pytest -q tests/test_chunking.py tests/test_pdf_service.py`：11 項相關測試全部通過。
- 本次僅進行診斷，未修改程式碼或預覽行為。

### 結果

- 若狀態文字顯示已解析 300 多頁，表格只出現來源頁碼 1–4 屬正常的前 10 個 chunk 預覽限制。
- 若狀態文字顯示僅解析 4 頁，則不正常，需取得實際狀態文字與 PDF 樣本進一步診斷。

## 2026-09-06T23:19:43+08:00

### 工作目標

- 評估讓使用者在網頁中檢視大型 PDF 每一頁內容的介面方案。

### 評估內容

- 比較一次顯示所有 chunk、提高預覽上限及依頁碼切換內容的操作與效能差異。
- 考量 300 多頁文件若一次載入全部資料，可能增加瀏覽器與 Gradio 表格負擔。
- 建議加入頁碼選擇器及上一頁／下一頁操作，並顯示指定頁面的所有相關 chunk。

### 驗證

- 確認現有 chunk 已保存來源頁碼，因此可依頁碼篩選而不需重新解析 PDF。
- 本次僅進行方案評估，未修改程式碼，因此未執行程式測試。

### 結果

- 可以支援每一頁檢視；建議採逐頁導覽方案，而非一次顯示整份 PDF 的所有 chunk。

## 2026-09-06T23:32:22+08:00

### 工作目標

- 在 PDF chunk 預覽加入逐頁檢視與上一頁／下一頁導覽。
- 放大預覽區域，改善大型 PDF 的閱讀操作。

### 完成內容

- 新增依 PDF 頁碼篩選所有相關 chunk 的 `preview_rows_for_page()`。
- 將完整 chunk 清單保存於獨立 Gradio state，不寫入設定匯出資料，也不需切頁時重新解析 PDF。
- 新增動態頁碼滑桿、上一頁、下一頁、目前頁數及該頁 chunk 數量提示。
- 支援無文字頁面提示，並限制頁碼導覽不超出文件範圍。
- 將 Gradio 介面改為滿版，預覽欄比例由 2 提高為 3，表格最大高度調整為 750px。
- 加寬內容欄並開啟表格搜尋功能。
- 更新 README 與目錄說明中的操作方式、模組責任及測試範圍。

### 測試與驗證

- 初次相關測試發現新增 helper 缺少清單結尾，補齊語法與遺漏斷言後重新驗證通過。
- `.venv/bin/pytest -q tests/test_chunking.py tests/test_ui.py`：11 項相關測試全部通過。
- `.venv/bin/pytest -q`：31 項完整測試全部通過，另有 1 項既有 Gradio 棄用警告。
- `.venv/bin/python -m compileall -q src tests`：通過。
- `.venv/bin/pip check`：通過，未發現相依衝突。
- `git diff --check`：通過。

### 結果

- 使用者現在可以跳至任一 PDF 頁面，或使用前後頁按鈕逐頁查看所有相關 chunk。
- 放大的預覽器可在大型文件中顯示更多內容，同時避免一次載入全部頁面到表格。

## 2026-09-06T23:34:35+08:00

### 工作目標

- 在 commit 規範中加入無檔案異動時免提交的例外條款。

### 完成內容

- 更新 `AGENTS.md` 第 1 項規範，將強制 commit 的條件明確限定為本次工作有檔案異動。
- 明定本次工作若沒有更動任何檔案，則不需要執行 `git commit`。

### 驗證

- 確認規範仍要求所有有檔案異動的獨立功能使用清楚訊息提交。
- 確認無檔案異動的例外條款語意清楚，且不影響其他開發規範。
- 本次僅修改 Markdown 文件，未變更程式碼，因此沒有需要執行的程式測試。

### 結果

- commit 規範已能區分有檔案異動與無檔案異動的工作情境。

## 2026-09-07T00:00:01+08:00

### 工作目標

- 在 PDF 頁面加入解析起始頁與結束頁設定。
- 讓 PyMuPDF4LLM 僅處理所選頁碼範圍，並限制逐頁預覽導覽範圍。

### 完成內容

- 在 PDF 上傳區新增「解析起始頁」與「解析結束頁」整數欄位，結束頁可留空代表文件末頁。
- 新增頁碼範圍驗證，涵蓋起始頁小於 1、結束頁小於起始頁及超出 PDF 總頁數。
- 將使用者頁碼轉為 PyMuPDF4LLM 所需的零起算 `pages` range，避免解析未選取頁面。
- 保留解析結果的原始 PDF 頁碼，並將起訖頁與實際解析頁數加入設定 state。
- 讓頁碼滑桿、直接跳頁及前後頁按鈕限制在所選解析範圍內。
- 擴充 mock 與真實 PDF 整合測試，驗證第 2–3 頁的選擇性解析。
- 更新 README、目錄說明與開發計畫中的操作方式、模組責任與測試說明。

### 測試與驗證

- 初次相關測試發現舊斷言將實際解析頁數誤當成最後頁碼，修正為分別驗證 `page_count`、`page_start` 與 `page_end`。
- `.venv/bin/pytest -q tests/test_pdf_service.py tests/test_ui.py`：17 項相關測試全部通過。
- `.venv/bin/pytest -q`：33 項完整測試全部通過，另有 1 項既有 Gradio 棄用警告。
- `.venv/bin/python -m compileall -q src tests`：通過。
- `.venv/bin/pip check`：通過，未發現相依衝突。
- `git diff --check`：通過。

### 結果

- 使用者可指定 PDF 解析起訖頁；結束頁留空時解析至末頁，無效範圍會顯示清楚錯誤。
- 大型 PDF 現在只解析選取範圍，逐頁預覽仍使用原始 PDF 頁碼並受該範圍限制。

## 2026-09-07T00:14:49+08:00

### 工作目標

- 將 PDF「解析結束頁」在上傳文件後自動預設為最後一頁。

### 完成內容

- 新增輕量的 `get_pdf_page_count()`，只開啟 PDF 取得總頁數，不執行全文解析。
- 新增 `initialize_page_range()`，上傳 PDF 後將起始頁設為 1、結束頁設為總頁數。
- 同步將起始／結束頁欄位最大值限制為 PDF 總頁數，清除檔案時重設欄位。
- 更新結束頁欄位標籤與上傳事件，顯示偵測到的總頁數及預設頁碼。
- 更新 README、目錄說明與開發計畫中的預設行為、模組責任與測試說明。

### 測試與驗證

- 真實三頁 PDF 測試確認 `get_pdf_page_count()` 回傳 3。
- 模擬 326 頁 PDF 測試確認結束頁自動填入 326，兩個欄位最大值均為 326。
- `.venv/bin/pytest -q`：34 項完整測試全部通過，另有 1 項既有 Gradio 棄用警告。
- `.venv/bin/python -m compileall -q src tests`：通過。
- `.venv/bin/pip check` 與 `git diff --check`：通過。

### 結果

- 使用者上傳 PDF 後，解析結束頁會自動顯示文件最後一頁，仍可手動縮小解析範圍。
- 系統只讀取頁數來初始化欄位，不會在上傳事件中重複執行完整 PyMuPDF4LLM 解析。

## 2026-09-07T01:21:10+08:00

### 工作目標

- 將 PyMuPDF4LLM 產生的 Markdown 改為依文件結構切分 chunk，不再一律依固定字元位置切割。

### 完成內容

- 新增 Markdown 標題階層辨識，依 H1–H6 建立章節上下文，並將完整標題路徑附加至各 chunk。
- 以空白行辨識內容區塊，盡量保持段落、清單、表格及 fenced code block 完整；程式碼內的 `#` 註解不會被誤判為標題。
- 在不超過 `chunk_size` 的前提下合併相鄰區塊；一般 overlap 僅保留可完整容納的前一區塊。
- 只有單一 Markdown 區塊本身超過 `chunk_size` 時，才退回帶有 `chunk_overlap` 的字元切分，同時保留章節上下文及來源 PDF 頁碼。
- PDF 預覽表新增「章節」欄，以 `父章節 > 子章節` 顯示 chunk 所屬結構。
- 更新 README、專案需求、開發計畫與目錄說明，記錄新切分規則、預覽內容及測試範圍。
- 新增標題階層、跨頁來源、fenced code、程式碼註解、Markdown 表格、超長區塊 fallback 與章節預覽測試。

### 測試與驗證

- `.venv/bin/pytest -q tests/test_chunking.py tests/test_ui.py`：17 項相關測試全部通過，另有 1 項既有 Gradio 棄用警告。
- `.venv/bin/pytest -q`：40 項完整測試全部通過，另有 1 項既有 Gradio 棄用警告。
- `.venv/bin/python -m compileall -q src tests`：通過。
- `.venv/bin/pip check`：通過，未發現相依衝突。
- `git diff --check`：通過。

### 結果

- PDF Markdown 現在優先沿著章節與完整內容區塊切分，降低表格、程式碼與段落在不必要位置被截斷的情況。
- 使用者可在放大的逐頁預覽器中直接查看每個 chunk 的章節路徑、來源頁碼、字元數與完整內容。

## 2026-09-07T01:38:54+08:00

### 工作目標

- 在 PyMuPDF4LLM 轉換 PDF 時排除頁首與頁尾，避免重複內容進入 chunk。

### 完成內容

- 在 `extract_pdf()` 呼叫 PyMuPDF4LLM 時加入原生 `header=False` 與 `footer=False` 選項。
- 使用目前啟用的 PyMuPDF Layout 模型辨識並排除 `page-header` 與 `page-footer`，可處理常見重複頁首及變動頁碼，不採固定座標裁切或事後文字比對。
- 更新 mock 單元測試，確認解析器收到兩項排除參數。
- 調整真實 PDF 測試樣本，使正文位於內容區並包含具代表性的多行文字。
- 新增真實三頁 PDF 整合測試，確認重複頁首與各頁 footer 頁碼被移除，中央正文仍完整保留。
- 更新 README、專案需求、開發計畫與目錄說明，記錄預設排除行為、layout 判定特性及測試範圍。

### 測試與驗證

- `.venv/bin/pytest -q tests/test_pdf_service.py`：11 項 PDF 解析測試全部通過。
- `.venv/bin/pytest -q`：41 項完整測試全部通過，另有 1 項既有 Gradio 棄用警告。
- `.venv/bin/python -m compileall -q src tests`：通過。
- `.venv/bin/pip check`：通過，未發現相依衝突。
- `git diff --check`：通過。

### 結果

- PDF 轉換現在預設不輸出 layout 模型判定的頁首與頁尾，可降低重複導覽文字及頁碼對 Markdown chunk 的干擾。
- 排除結果依文件版面分類決定；特殊排版仍應透過逐頁預覽確認正文未被誤判。
