# 專案需求：PDF GraphRAG 測試工具

## 1. 專案目標

建立一套在電腦上執行的 Gradio 介面。使用者可上傳 PDF、調整模型與 chunk 參數、將內容寫入 Neo4j 知識圖譜，並直接提問測試 GraphRAG 效果。

操作流程：連線設定 → 上傳 PDF → 調整參數 → 預覽 chunk → 建立圖譜 → 提問與查看來源。

## 2. 使用範圍

- MVP 為單人、本機瀏覽器工具，介面使用繁體中文。
- 支援繁體中文與英文的文字型 PDF。
- Neo4j 可使用本機或遠端服務。
- LLM 與 embedding 可連接雲端或本機相容端點。
- Password、API key 與模型／連線欄位保存在本機 `.env`；建圖紀錄與問答結果使用 JSON，不使用 SQLite。
- OCR、複雜表格、圖片理解、多人權限及公開部署不列入 MVP。

## 3. 介面功能

| 分頁 | 功能 |
|---|---|
| 連線設定 | 設定並測試 Neo4j、建圖 LLM、embedding 與問答 LLM |
| PDF 與參數 | 上傳 PDF、設定解析頁碼與 chunk 參數、逐頁預覽文字 |
| 建圖 | 開始、取消或重試工作，顯示進度、錯誤及節點／關係數 |
| 問答測試 | 選擇建圖結果、輸入問題、查看答案、來源頁碼與檢索內容 |
| 歷史紀錄 | 查看、比較、匯出或刪除建圖與問答結果 |

## 4. 可調參數

### 建圖參數

- 建圖 LLM：端點、模型名稱、temperature、最大輸出 tokens；設定集中在建圖頁並實際傳入規劃與抽取 API。
- Embedding：端點、模型名稱與批次大小。
- Chunk：固定字元切分、`chunk_size`、`chunk_overlap`、頁面範圍。
- 圖譜：實體類型、關係類型與抽取提示。
- 執行：逾時、重試次數與模型請求並行數。

初始建議值為 `chunk_size = 1500` 字元、`chunk_overlap = 200` 字元。介面必須標示計量單位，並驗證 `0 ≤ chunk_overlap < chunk_size`。切分時應使用固定字元視窗，並保留 overlap 及來源頁碼。

### 問答參數

- 問答 LLM 與生成參數。
- 檢索模式：向量 RAG 或 GraphRAG。
- `top_k`、圖譜擴展深度及上下文 token 預算。

問答時必須使用該次建圖所記錄的 embedding 模型。更換 embedding、chunk 或建圖模型時須建立新的建圖結果；只更換問答模型或檢索參數時不需重建。

## 5. 功能需求

1. 可分別測試 Neo4j、LLM 與 embedding 連線。
2. 可驗證 PDF 格式，排除每頁上方與下方的頁首頁尾區域，並顯示頁數、文字與無法解析的頁面。
3. 調整 chunk 後可預覽內容、長度與來源頁碼，正式建圖使用相同切分結果。
4. 建圖前可在獨立區塊選擇抽取 LLM，由 LLM 分批分析所有 chunks、以較小整合批次和精簡中間欄位階層整合候選類型，並規劃具捲動預覽的可編輯實體／關係 schema；確認後在另一區塊選擇 Embedding 模型、批次抽取、顯示來源並自動交易式匯入 Neo4j。
5. 每次建圖保存獨立的參數快照、狀態、耗時及圖譜統計。
6. 建圖結果互相隔離，切換或刪除其中一次不影響其他結果。
7. 問答只使用選定建圖結果的資料，並顯示檔名、PDF 頁碼與來源文字。
8. 查無足夠證據時明確回覆資料不足，不虛構答案或頁碼。
9. 支援建圖取消、失敗重試、歷史查詢與安全刪除。
10. 啟動時自動讀取 `.env`，修改連線或模型欄位時自動儲存，也可手動重新讀取。
11. 實驗設定及結果可匯出為 JSON，但不得包含密碼或 API key。

## 6. JSON 儲存需求

- JSON 使用 UTF-8，並包含 `schema_version`、ID 與更新時間。
- 保存 PDF 索引、設定預設值、建圖 manifest、checkpoint 與問答結果。
- 採原子寫入，避免程式中斷造成檔案損毀。
- MVP 僅允許一個應用程序寫入同一資料目錄。
- 程式重啟後可讀取既有結果；中斷中的工作標記為 `interrupted`，可手動重試。

## 7. 驗收條件

1. 使用者能完成「上傳 PDF → 預覽 chunk → 建圖 → 提問 → 查看來源」流程。
2. 使用兩組模型或 chunk 參數建圖時，系統保存兩筆互不影響的結果。
3. 相同問題可比較向量 RAG 與 GraphRAG，並查看各自答案、來源及耗時。
4. 更換 embedding 時建立新索引，不混用不同模型產生的向量。
5. 無效參數、連線失敗及 PDF 解析失敗均顯示清楚錯誤。
6. 重啟後仍能讀取已完成的建圖與問答紀錄。
7. 匯出的 JSON 與日誌不包含密碼或 API key。

## 8. 待確認事項

- 主要作業系統與電腦硬體規格。
- Neo4j 的部署方式與版本。
- 使用的模型服務及 API 格式。
- 代表性 PDF 樣本、文件大小與語言。
