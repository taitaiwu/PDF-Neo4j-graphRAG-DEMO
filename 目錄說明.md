# 專案目錄說明

本專案是一個以 Python 與 Gradio 開發的本機 PDF GraphRAG 測試工具。目前已完成介面、PyMuPDF4LLM PDF 解析、chunk 切分與預覽、環境設定儲存，以及 JSON 設定匯出的初版功能；Neo4j 寫入與 GraphRAG 問答後端尚待後續接入。

## 目錄結構

```text
.
├── src/
│   ├── app.py
│   └── manual_graphrag/
│       ├── __init__.py
│       ├── chunking.py
│       ├── config.py
│       ├── env_store.py
│       ├── pdf_service.py
│       ├── storage.py
│       └── ui.py
├── tests/
│   ├── test_chunking.py
│   ├── test_pdf_service.py
│   ├── test_config.py
│   ├── test_env_store.py
│   ├── test_storage.py
│   └── test_ui.py
├── .env.example
├── .gitignore
├── AGENTS.md
├── pytest.ini
├── requirements.txt
├── README.md
├── 專案需求.md
├── 開發進度.md
├── 開發計畫.md
├── start.sh
└── 目錄說明.md
```

## 原始程式

| 檔案 | 作用 |
|---|---|
| `src/app.py` | 應用程式啟動入口；建立 Gradio 介面、限制單一佇列並在本機 `127.0.0.1` 啟動服務。 |
| `src/manual_graphrag/__init__.py` | `manual_graphrag` Python 套件的初始化檔，並定義專案版本。 |
| `src/manual_graphrag/ui.py` | 建立 Gradio 各分頁、介面元件與事件處理；整合連線設定、PDF 起訖頁設定、逐頁導覽與大型 chunk 預覽器、設定匯出、建圖與問答入口。 |
| `src/manual_graphrag/config.py` | 定義建圖參數模型與驗證規則，並在顯示或匯出設定前移除密碼、API key 等機密值。 |
| `src/manual_graphrag/env_store.py` | 讀取與寫入 `.env` 連線及模型設定；使用暫存檔及原子替換來降低檔案損毀風險。 |
| `src/manual_graphrag/pdf_service.py` | 使用 PyMuPDF4LLM 驗證並將指定頁碼範圍解析為 Markdown，同時保留來源頁碼並回報空白或無法擷取文字的頁面。 |
| `src/manual_graphrag/chunking.py` | 定義頁面文字與 chunk 資料結構，依字元數及重疊長度切分文字、保留來源頁碼，並支援依 PDF 頁碼篩選預覽。 |
| `src/manual_graphrag/storage.py` | 提供帶有 `schema_version` 的 JSON 讀寫功能；寫入時採用原子替換。 |

## 測試

| 檔案 | 作用 |
|---|---|
| `tests/test_chunking.py` | 測試 chunk 切分、來源頁碼保留、逐頁篩選、空白頁與無效 overlap 處理。 |
| `tests/test_pdf_service.py` | 測試 PyMuPDF4LLM 指定頁碼範圍解析、Markdown 與頁碼保留、空白頁、輸入驗證及解析失敗處理。 |
| `tests/test_config.py` | 測試建圖參數驗證以及機密設定過濾。 |
| `tests/test_env_store.py` | 測試 `.env` 儲存往返、特殊字元與非管理欄位保留。 |
| `tests/test_storage.py` | 測試 JSON 寫入、讀回與 schema 版本檢查。 |
| `tests/test_ui.py` | 測試 Gradio 應用建立、PDF 解析範圍初始化與頁碼導覽、安全的連線設定預覽及環境設定儲存。 |

## 設定與說明文件

| 檔案 | 作用 |
|---|---|
| `.env.example` | 環境設定範本，列出 Neo4j、模型 API 與模型名稱所需欄位；可複製為 `.env` 後填入本機設定。 |
| `.gitignore` | 排除虛擬環境、Python 快取、測試快取、`.env` 與執行資料等不應進入版本控制的內容。 |
| `requirements.txt` | 宣告 Gradio、PyMuPDF4LLM 與 pytest 的 Python 相依套件及版本範圍。 |
| `pytest.ini` | pytest 設定；將 `src` 加入 Python 模組搜尋路徑。 |
| `README.md` | 專案簡介、安裝、啟動與測試方式，並提醒機密資料與匯出資料的保存位置。 |
| `start.sh` | Linux 一鍵啟動腳本；自動準備 Python 虛擬環境、安裝套件、建立 `.env` 並啟動網站。 |
| `專案需求.md` | 記錄專案目標、MVP 範圍、介面功能、儲存要求與驗收條件。 |
| `開發計畫.md` | 說明技術架構、模組規劃、開發階段、測試策略與已知限制。 |
| `AGENTS.md` | 定義本專案的開發與交付規範，包含提交、測試及驗證要求。 |
| `目錄說明.md` | 本文件；整理專案目錄與各檔案的主要用途。 |

## 執行時產生的內容

以下項目不屬於原始程式，且已由 `.gitignore` 排除：

| 路徑 | 作用 |
|---|---|
| `.env` | 保存本機連線、模型、密碼與 API key；含機密資料，不可提交或分享。 |
| `data/` | 保存應用匯出的設定 JSON，未來也將用於文件索引、建圖紀錄與問答結果。 |
| `.venv/` | Python 虛擬環境與已安裝的相依套件。 |
| `__pycache__/`、`*.pyc` | Python 執行時產生的 bytecode 快取。 |
| `.pytest_cache/` | pytest 產生的測試快取與執行資訊。 |
