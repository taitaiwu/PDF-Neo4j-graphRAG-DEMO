# 專案目錄說明

本專案是一個以 Python 與 Gradio 開發的本機 PDF GraphRAG 測試工具。目前已完成介面、PDF 解析、chunk 預覽、知識圖譜抽取與 Neo4j 匯入、混合向量／圖譜擴展問答，以及多 PDF 獨立摘要、LLM 文件路由、自動生題、並行測試與分文件成績統計。

## 目錄結構

```text
.
├── src/
│   ├── app.py
│   └── manual_graphrag/
│       ├── __init__.py
│       ├── chunking.py
│       ├── config.py
│       ├── env_store.py
│       ├── evaluation_service.py
│       ├── pdf_service.py
│       ├── project_store.py
│       ├── graph_service.py
│       ├── neo4j_service.py
│       ├── qa_service.py
│       ├── storage.py
│       └── ui.py
├── tests/
│   ├── test_app.py
│   ├── test_chunking.py
│   ├── test_pdf_service.py
│   ├── test_project_store.py
│   ├── test_graph_service.py
│   ├── test_neo4j_service.py
│   ├── test_config.py
│   ├── test_env_store.py
│   ├── test_qa_service.py
│   ├── test_evaluation_service.py
│   ├── test_storage.py
│   └── test_ui.py
├── .env.example
├── .dockerignore
├── .gitignore
├── Dockerfile
├── AGENTS.md
├── pytest.ini
├── requirements.txt
├── README.md
├── 專案需求.md
├── start.sh
└── 目錄說明.md
```

## 原始程式

| 檔案 | 作用 |
|---|---|
| `src/app.py` | 應用程式啟動入口；建立 Gradio 介面、限制單一佇列並在本機 `127.0.0.1` 啟動服務。 |
| `src/manual_graphrag/__init__.py` | `manual_graphrag` Python 套件的初始化檔，並定義專案版本。 |
| `src/manual_graphrag/ui.py` | 建立 Gradio 各分頁與事件處理；整合服務設定、多 PDF 管理、獨立摘要建立與結果顯示、Schema 規劃、平行抽取、Neo4j 匯入、問答，以及每份 PDF 各生 N 題、可分別設定生題／測試並行數和分 PDF 顯示測試成績。 |
| `src/manual_graphrag/config.py` | 定義建圖參數模型與驗證規則，並在顯示或匯出設定前移除密碼、API key 等機密值；模型回覆不再設定輸出 token 上限。 |
| `src/manual_graphrag/evaluation_service.py` | 依指定 PDF chunks 建立路由摘要，並產生問題、標準答案、來源頁碼與來源文件，並以模型比較 RAG 實際答案進行語意判分。 |
| `src/manual_graphrag/env_store.py` | 讀取與寫入 `.env` 連線設定；使用暫存檔及原子替換來降低檔案損毀風險。 |
| `src/manual_graphrag/project_store.py` | 建立、列出、載入、刪除及原子保存獨立專案；管理 PDF 副本、工作設定與問答歷史，並禁止把模型 API endpoint／key 寫入 `project.json`。 |
| `src/manual_graphrag/pdf_service.py` | 讀取 PDF 總頁數，並使用 PyMuPDF 擷取指定頁碼範圍的排序純文字，排除頁面上下各 8% 的頁首頁尾區域，同時保留來源頁碼並回報空白或無法擷取文字的頁面。 |
| `src/manual_graphrag/neo4j_service.py` | 驗證 Neo4j 連線設定並測試指定 Database，使用官方 driver 以單一 write transaction 建立文件、實體節點、抽取關係，以及原文 chunk、實體與關係的向量證據；建立向量與 CJK 全文索引，並以官方 HybridCypherRetriever 融合兩種檢索結果；支援依命中 chunk 與實體擴展圖譜證據及回報實際寫入數量。 |
| `src/manual_graphrag/graph_service.py` | 測試 OpenAI-compatible 模型端點並呼叫不含 `max_tokens` 的 chat completions；平行規劃 Schema 與批次抽取，且 Schema 類型及抽取實體／關係均無數量上限。 |
| `src/manual_graphrag/qa_service.py` | 建立原文 chunk、實體與關係向量，透過 Neo4j 混合檢索與圖譜擴展取得證據，再呼叫不含 `max_tokens` 的問答 LLM 生成附來源答案。 |
| `src/manual_graphrag/chunking.py` | 定義頁面文字與 chunk 資料結構，依字元數及重疊長度切分文字、保留來源頁碼，並支援依 PDF 頁碼篩選預覽。 |
| `src/manual_graphrag/storage.py` | 提供帶有 `schema_version` 的 JSON 讀寫功能；寫入時採用原子替換。 |

## 測試

| 檔案 | 作用 |
|---|---|
| `tests/test_app.py` | 測試本機與 Docker 環境的 Gradio 監聽設定。 |
| `tests/test_chunking.py` | 測試固定字元 chunk、來源頁碼保留、逐頁篩選、空白頁與無效 overlap 處理。 |
| `tests/test_neo4j_service.py` | 測試 Neo4j 欄位驗證、driver 連線、交易式 Cypher 寫入、數量回報與錯誤包裝。 |
| `tests/test_graph_service.py` | 測試 Schema 驗證、分批分析、階層整合、重試、模型 JSON 回應、請求不含 `max_tokens`、批次抽取、無類型數量上限、去重及來源合併。 |
| `tests/test_project_store.py` | 測試專案建立、清單、設定往返、模型憑證過濾、文件複製、問答追加與名稱驗證。 |
| `tests/test_qa_service.py` | 測試問答 chat completions 請求不包含 `max_tokens`。 |
| `tests/test_pdf_service.py` | 測試 PDF 總頁數讀取、PyMuPDF 指定頁碼範圍純文字擷取、頁首頁尾區域排除與頁碼保留、空白頁、輸入驗證及解析失敗處理。 |
| `tests/test_config.py` | 測試建圖參數驗證以及機密設定過濾。 |
| `tests/test_evaluation_service.py` | 測試自動題目產生、來源文件／chunk 驗證及答案判分。 |
| `tests/test_env_store.py` | 測試 `.env` 儲存往返、特殊字元與非管理欄位保留。 |
| `tests/test_storage.py` | 測試 JSON 寫入、讀回與 schema 版本檢查。 |
| `tests/test_ui.py` | 測試 Gradio 應用、專案選單刷新、多 PDF 管理、模型服務、建圖、每份 PDF 各生 N 題、評估並行數、分文件測試摘要及環境設定儲存。 |

## 設定與說明文件

| 檔案 | 作用 |
|---|---|
| `.env.example` | 環境設定範本，分別列出 Neo4j、OpenAI、Ollama 與 Voyage 的 API Base URL／API Key。 |
| `config/model_settings.yaml` | 保存 OpenAI／Voyage 模型白名單、目前服務來源、模型選擇與 Ollama 勾選清單。 |
| `.dockerignore` | 排除機密設定、執行資料、測試、PDF、虛擬環境及版本控制資料，縮小 Docker 建置內容。 |
| `.gitignore` | 排除虛擬環境、Python 快取、測試快取、`.env` 與執行資料等不應進入版本控制的內容。 |
| `Dockerfile` | 使用 Python 3.12 建置 Gradio 執行映像，對外提供 7860 連接埠。 |
| `requirements.txt` | 宣告 Gradio、PyMuPDF、Neo4j driver 與 pytest 的 Python 相依套件及版本範圍。 |
| `pytest.ini` | pytest 設定；將 `src` 加入 Python 模組搜尋路徑。 |
| `README.md` | 專案簡介、安裝、啟動與測試方式，並提醒機密資料與匯出資料的保存位置。 |
| `start.sh` | Linux 一鍵啟動腳本；自動準備 Python 虛擬環境、安裝套件、建立 `.env` 並啟動網站。 |
| `專案需求.md` | 記錄專案目標、MVP 範圍、介面功能、儲存要求與驗收條件。 |
| `AGENTS.md` | 定義本專案的開發與交付規範，包含提交、測試及驗證要求。 |
| `目錄說明.md` | 本文件；整理專案目錄與各檔案的主要用途。 |

## 執行時產生的內容

以下項目不屬於原始程式，且已由 `.gitignore` 排除：

| 路徑 | 作用 |
|---|---|
| `.env` | 保存本機連線、密碼與 API key；含機密資料，不可提交或分享。 |
| `data/` | 保存應用匯出資料，以及 `projects/<project-id>/` 下的非模型憑證專案設定、PDF 副本與問答紀錄；模型端點及 API key 統一由 `.env` 管理。 |
| `.venv/` | Python 虛擬環境與已安裝的相依套件。 |
| `__pycache__/`、`*.pyc` | Python 執行時產生的 bytecode 快取。 |
| `.pytest_cache/` | pytest 產生的測試快取與執行資訊。 |
